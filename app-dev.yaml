runtime: python39

env_variables:
  DISCORD_DEV_TOKEN: "dev_token"
  DATABASE_URL: "cloud_sql_connection_string"
  ENVIRONMENT: "development"

automatic_scaling:
  min_instances: 1
  max_instances: 5 #?
  target_cpu_utilization: 0.6

handlers:
- url: /.*
  script: auto